# Roadmap

- [ ] Fix bugs
- [ ] Create legacy branch

## Issues

- [ ] super+w not working (may actually switch to a script to randomly switch instead).
- [ ] switch workspaces not working.
- [ ] Pop-ups getting stacked instead of killing previous.
- [ ] Power pop-up not locking (rest works?).

## Legacy

Make the main branch `legacy` and this one the main branch when everything
working and tested.

- Make sure the script install is still works from the git via curl.
